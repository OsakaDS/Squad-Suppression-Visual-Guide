server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  gzip on;
  gzip_types text/html text/css application/javascript image/svg+xml text/csv;
  gzip_min_length 1024;
  charset utf-8;
  location / { try_files $uri $uri/ =404; }
}
